//initiera variabel för att lagra element som refererar till form
const form = document.querySelector(".submit-form");

console.log(`form: ${form}`);

//
console.log(form);
const name = form.querySelector(".userName").value;
console.log(`name: ${name}`);
